Run `python pipelines/flight-runner/scripts/deploy.py $ARGUMENTS` and report the result.

No flag: uploads `config.json` + `specification.json` only (use between tuning iterations).
`--full`: uploads config + all source files (use after source code changes).
