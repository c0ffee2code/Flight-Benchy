Run `python -m mpremote connect COM7 run pipelines/flight-runner/scripts/reset_position.py` and report the result.

Wait for "Done -- M1 end at restrictor." before confirming completion. Takes ~5 seconds total.
