Run `python -m pytest tests/ -v $ARGUMENTS` from the project root and report the result.

No arguments: run the full suite (25 tests).
Pass a file or test name to narrow: e.g. `tests/test_control.py` or `-k feedforward`.