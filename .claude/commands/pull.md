Run `python .claude/commands/pull.py $ARGUMENTS` and report the result.

No flag: lists new runs on the SD card and asks for confirmation before transferring.
`--yes`: skips confirmation, pulls all new runs immediately.
